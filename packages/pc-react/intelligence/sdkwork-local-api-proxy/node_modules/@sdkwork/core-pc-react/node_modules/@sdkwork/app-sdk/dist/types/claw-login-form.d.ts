export interface ClawLoginForm {
    /** Claw username. */
    username: string;
    /** Claw password. */
    password: string;
    /** Claw key. */
    clawKey?: string;
    /** Client semantic version used to negotiate follow-up QR code capabilities. */
    clientVersion?: string;
    /** Remember me. */
    rememberMe?: boolean;
}
//# sourceMappingURL=claw-login-form.d.ts.map